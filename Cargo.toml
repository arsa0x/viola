[package]
name = "viola_script"
version = "0.1.0"
edition = "2024"

# [[bench]]
# harness = false
# name = "viola_script_bench"

[dependencies]

[dev-dependencies]
criterion = { version = "0.8.2", features = ["async_tokio"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
